#include <stdio.h>

int main(int argc, char const *argv[])
{
	int N, M, T;
	char n[7][7];
	while(scanf("%d%d%d", &N, &M, &T) && N + M + T){
		for(int i = 0; i < N; i++){
			for(int j = 0; j < M; j++){
				scanf("%c", &a[i][j]);
			}
		}

		

	}
	return 0;
}