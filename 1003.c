#include<stdio.h>

int main(){
	int T, N;
	int a[100001];

	scanf("%d", &T);

	for(int i = 1; i <= T; i++){
		scanf("%d", &N);

		for(int i = 0; i <= N-1; i++)
			scanf("%d", &a[i]);

		maxsum = a[0];

		for(int i = 0; i <= N-1; i++){
			if(sum > max )
		}


	}

	return 0;
}